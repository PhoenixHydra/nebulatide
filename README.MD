How to Run
You can run any of these commands in your terminal (inside the 'mobile' folder):

Web: npx expo start --web

Android: npx expo run:android (Requires Android device/emulator)


iOS: npx expo run:ios (Requires Mac & iOS Simulator/device)
